__pycache__/
*.pyc
.DS_Store
._*
# run products, not inputs
*.xtc
*.trr
*.edr
*.cpt
*.log
\#*\#

# a build's output, not an input. One of these is 78 MB.
run_*/
mdout.mdp
# written by ./f127 check and ./f127 new, local to whoever ran them
/compat.json
/system.cfg
/system.json
*.tar.gz
# local backups made while editing
bak/
